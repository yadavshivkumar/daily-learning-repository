a = (1 ,2, 3, 4, 5)

a = (1,)   # Tuple with a single element must have a comma

a[0] = 2  # Tuples are immutable, this will raise an error

print(type(a)) # Tuple is immutable, cannot change its elements

