#include<stdio.h>
int main ( )
{
    int a;
    int b;
    int c; 
    int sum;
    return 0;


}

// constant variables
// Rules for naming variables
// 1. Variable names can contain letters (both uppercase and lowercase), digits, and underscores (_).
// 2. Variable names must begin with a letter (A-Z, a-z) or an underscore (_). They cannot start with a digit (0-9).
// 3. Variable names are case-sensitive. For example, myVariable and myvariable are considered different variables.
// 4. Variable names cannot be the same as C keywords (reserved words) such as int, return, if, else, etc.
// 5. Variable names should not contain spaces or special characters (e.g., !, @, #, $, %, ^, &, *, etc.).
// 6. Variable names should be descriptive and meaningful to enhance code readability.
// 7. There is no strict limit on the length of variable names, but it's advisable to keep them reasonably short for practicality.
// 8. Avoid using single-character variable names (except for loop counters) to improve code clarity.
// 9. Use underscores to separate words in variable names for better readability (e.g., my_variable_name).
// 10. Follow a consistent naming convention throughout your code (e.g., camelCase, snake_case, etc.).// 11. Avoid using global variables unless necessary, as they can lead to code that is difficult to maintain and debug.            
// 12. Always initialize variables before using them to avoid undefined behavior.
// 13. Use comments to explain the purpose of variables, especially if their names are not self-explanatory.
// 14. Avoid using abbreviations that are not widely understood, as they can make the code less readable.
// 15. Regularly review and refactor variable names to ensure they remain relevant and clear as the code evolves.
// 16. When working in a team, agree on a common set of variable naming conventions to  ensure consistency across the codebase.         
// 17. Avoid using reserved prefixes or suffixes that may conflict with library or framework conventions.
// 18. Consider the scope of the variable when naming it; use more specific names for variables with a broader scope to avoid confusion.
// 19. Avoid using magic numbers (hard-coded values) in your code; instead, use named constants or variables to improve clarity.
// 20. Regularly test and validate your code to ensure that variable names and their usage align with the intended functionality.           
// 21. Use meaningful names for variables that reflect their purpose or the data they hold, making it easier for others (and yourself) to understand the code later.
// 22. Avoid using overly generic names like temp, data, or value, as they do not convey any specific meaning about the variable's role in the program.
// 23. When working with arrays or collections, consider using plural names to indicate that they hold multiple values (e.g., numbers, items).
// 24. Be mindful of cultural differences and avoid using variable names that may be offensive or inappropriate in certain contexts.
// 25. Use consistent naming conventions for different types of variables (e.g., use camelCase for variables, PascalCase for types, etc.) to enhance code readability.
// 26. Avoid using abbreviations that may be unclear or ambiguous, as they can lead to misunderstandings about the variable's purpose.
// 27. When working with boolean variables, consider using prefixes like is, has, or can to indicate their true/false nature (e.g., isActive, hasPermission).
// 28. Regularly review and refactor your code to ensure that variable names remain relevant and accurately reflect their purpose as the code evolves.
// 29. When working in a team, establish and document a set of coding standards and guidelines that include variable naming conventions to ensure consistency across the codebase.
// 30. Use comments to explain the purpose of complex or non-obvious variable names, especially if they are not self-explanatory.
// 31. Avoid using single-character variable names (except for loop counters) to improve code clarity and maintainability.
// 32. When working with multiple variables of the same type, consider using a common prefix or suffix to group them logically (e.g., userName, userEmail, userAge).
// 33. Be cautious when using underscores in variable names, as they can sometimes be confused with spaces or other characters.
// 34. Avoid using variable names that are too similar to each other, as this can lead to confusion and errors in the code.
// 35. When working with constants, use uppercase letters and underscores to distinguish them from regular variables (e.g., MAX_VALUE, DEFAULT_TIMEOUT).
// 36. Avoid using reserved words or keywords as variable names, as this can lead to syntax errors and confusion.
// 37. When working with functions, consider using descriptive names for parameters to indicate their purpose and expected values.
// 38. Regularly test and validate your code to ensure that variable names and their usage align with the intended functionality.
// 39. Use meaningful names for variables that reflect their purpose or the data they hold, making it easier for others (and yourself) to understand the code later.
// 40. Avoid using overly generic names like temp, data, or value, as they do not